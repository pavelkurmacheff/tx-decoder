import {OInchAbiDecoder} from './abi-decoder';

describe('1inch abi decoder', () => {
    let oInchAbiDecoder: OInchAbiDecoder;

    beforeEach(() => {
        oInchAbiDecoder = new OInchAbiDecoder();
    });

    it('CallData parse', () => {

    });
});
